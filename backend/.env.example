# Copy this file to `.env` and fill in real values.
# `.env` itself is git-ignored so secrets never get committed.

PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=jsonplaceholder_db

DB_CONNECTION_LIMIT=10
